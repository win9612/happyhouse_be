<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper
	namespace="com.ssafy.happyhouse.model.mapper.InterestMapper">

	<!-- 관심지역 등록 -->
	<insert id="addInterest" parameterType="InterestDto">
		insert into interest
		values (#{no}, #{aptCode})
	</insert>

	<!-- 해당 유저의 관심지역 목록 불러오기 -->
	<select id="selectList" parameterType="int"
		resultType="com.ssafy.happyhouse.model.HouseInfoDto">
		select interest.aptCode, aptName from houseinfo join interest on
		houseinfo.aptCode = interest.aptCode where interest.no = #{no};
	</select>

	<!-- 해당 지역이 관심지역인지 확인, 0이면 아닌거임 -->
	<select id="checkInterest" parameterType="int" resultType="int">
		select count(*)
		from interest
		where no=#{no} and aptCode=#{aptCode}
	</select>

	<!-- 관심지역 삭제 -->
	<delete id="deleteInterest" parameterType="int">
		delete from interest
		where no=#{no} and aptCode=#{aptCode}
	</delete>

</mapper>